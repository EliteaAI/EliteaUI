export const BRAND_LOGO_VARIANTS = {
  // Square logo mark (sidebar, chat answer header, modal messages). Sized via `fontSize` like SvgIcon.
  Icon: 'icon',
  // Horizontal logo (onboarding). Fills its container.
  Wordmark: 'wordmark',
  // Elitea round avatar (chat messages, new chat welcome, onboarding). Size is set via `sx` width / height.
  Avatar: 'avatar',
};
