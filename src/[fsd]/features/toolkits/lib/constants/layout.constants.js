/**
 * Height of a panel header band, border included. The details action bar, the Indexes panel and the
 * Test page columns sit side by side, so they share one number rather than each deriving a near-miss
 * from its own padding — a fixed height makes drift a visible clip instead of a 4px offset.
 */
export const PANEL_HEADER_HEIGHT = '3.3125rem';

export const PANEL_GUTTER = '1.5rem';
