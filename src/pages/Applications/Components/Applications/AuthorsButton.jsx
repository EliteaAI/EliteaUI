import { AuthorsButton } from '@/pages/Common';

// Wrapper component for Applications-specific usage
export default function ApplicationsAuthorsButton() {
  return <AuthorsButton entityType="applications" />;
}
